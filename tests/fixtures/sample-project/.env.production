# Production configuration
NODE_ENV=production
# Deliberate type mismatch: .env has an integer, this one does not parse.
PORT="3000abc"
DATABASE_URL=postgres://prod.internal/db
REDIS_URL=redis://prod.internal:6379
JWT_SECRET=prod-only-secret
LOG_LEVEL=warn
